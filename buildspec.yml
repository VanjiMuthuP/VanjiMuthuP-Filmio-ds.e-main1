version: 0.2
phases:
  install:
    commands:
      - yarn install
  pre_build:
    commands:
      - yarn run build 
      - yarn build storybook         
  build:
    commands:
      - echo "build complete"
artifacts:
  files:
    - "*/"
